name: Release SDK
on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version to release"
        required: true
jobs:
  release:
    name: Release
    runs-on: macOS-11
    steps:
      - name: Check out repository
        uses: actions/checkout@v2
        with:
          fetch-depth: 0